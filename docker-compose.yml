networks:
  portal-network:
    driver: bridge

volumes:
  db_data:
  qdrant_data:
  n8n_data:
  caddy_data:
  caddy_config:
  ollama_data:

services:

  # ── Reverse proxy + SSL automàtic ────────────────────────────────────────
  caddy:
    image: caddy:2-alpine
    container_name: portal_caddy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    environment:
      - BASE_DOMAIN=${DOMAIN_BASE:-localhost}
      - CLOUDFLARE_API_TOKEN=${CLOUDFLARE_API_TOKEN:-}
    volumes:
      - ./docker/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    networks:
      - portal-network
    depends_on:
      frontend:
        condition: service_healthy
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "caddy", "version"]
      interval: 30s
      timeout: 10s
      retries: 3

  # ── Frontend Next.js ──────────────────────────────────────────────────────
  frontend:
    image: portalamg-frontend:latest
    user: root
    container_name: portal_frontend
    restart: unless-stopped
    command: sh -c "npm install && npm run dev"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    expose:
      - "3000"
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=${NODE_ENV:-development}
      - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL:-http://localhost:4000}
    networks:
      - portal-network
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "true"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  # ── Backend Node.js / Express ─────────────────────────────────────────────
  backend:
    image: portalamg-backend:latest
    user: root
    container_name: portal_backend
    restart: unless-stopped
    command: sh -c "npm install && npx prisma generate && npx ts-node-dev --respawn --transpile-only src/index.ts"
    volumes:
      - ./backend:/app
      - /app/node_modules
      - ./gcs-credentials.json:/app/gcs-credentials.json:ro
    expose:
      - "4000"
    # Per accés directe en desenvolupament:
    ports:
      - "4000:4000"
    networks:
      - portal-network
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "true"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  # ── PostgreSQL ────────────────────────────────────────────────────────────
  db:
    image: postgres:15-alpine
    container_name: portal_db
    restart: unless-stopped
    ports:
      - "5434:5432"
    environment:
      - POSTGRES_DB=portal_db
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=password
    volumes:
      - db_data:/var/lib/postgresql/data
    networks:
      - portal-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U user -d portal_db"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ── IA / RAG (Python / FastAPI) ───────────────────────────────────────────
  ai:
    image: portalamg-ai:latest
    container_name: portal_ai
    restart: unless-stopped
    expose:
      - "8000"
    ports:
      - "8000:8000"
    environment:
      - IA_PROVIDER_QUALITY=${IA_PROVIDER_QUALITY:-groq}
      - IA_PROVIDER_FAST=${IA_PROVIDER_FAST:-groq}
      - IA_PROVIDER_LOCAL=${IA_PROVIDER_LOCAL:-ollama}
      - MODEL_QUALITY=${MODEL_QUALITY:-llama-3.3-70b}
      - MODEL_FAST=${MODEL_FAST:-llama-3.1-8b}
      - MODEL_LOCAL=${MODEL_LOCAL:-qwen3.6:35b-a3b}
      - GROQ_API_KEY=${GROQ_API_KEY}
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
      - OLLAMA_URL=${OLLAMA_URL:-http://ollama:11434}
      - QDRANT_URL=${QDRANT_URL:-http://qdrant:6333}
      - QDRANT_COLLECTION=${QDRANT_COLLECTION:-portal_rag}
      - GCS_BUCKET_NAME=${GCS_BUCKET_NAME}
      - GCS_PROJECT_ID=${GCS_PROJECT_ID}
      - GCS_KEY_FILE=/app/gcs-credentials.json
    volumes:
      - ./gcs-credentials.json:/app/gcs-credentials.json:ro
    networks:
      - portal-network
    depends_on:
      qdrant:
        condition: service_healthy
      ollama:
        condition: service_started
    healthcheck:
      test: ["CMD", "true"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  # ── Ollama (IA Local) ─────────────────────────────────────────────────────
  ollama:
    image: ollama/ollama:latest
    container_name: portal_ollama
    restart: unless-stopped
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    networks:
      - portal-network

  # ── n8n (automatitzacions) ────────────────────────────────────────────────
  n8n:
    image: n8nio/n8n:latest
    container_name: portal_n8n
    restart: unless-stopped
    ports:
      - "5678:5678"
    environment:
      - N8N_BASIC_AUTH_ACTIVE=true
      - N8N_BASIC_AUTH_USER=${N8N_BASIC_AUTH_USER:-admin}
      - N8N_BASIC_AUTH_PASSWORD=${N8N_BASIC_AUTH_PASSWORD}
      - WEBHOOK_URL=${BASE_URL:-http://localhost:3000}
      - GENERIC_TIMEZONE=Europe/Madrid
      - N8N_ENCRYPTION_KEY=${N8N_ENCRYPTION_KEY}
    volumes:
      - n8n_data:/home/node/.n8n
      - ./n8n/workflows:/workflows:ro
    networks:
      - portal-network
    healthcheck:
      test: ["CMD", "true"]
      interval: 30s
      timeout: 10s
      retries: 3

  # ── Qdrant (vector store per RAG) ─────────────────────────────────────────
  qdrant:
    image: qdrant/qdrant:latest
    container_name: portal_qdrant
    restart: unless-stopped
    expose:
      - "6333"
    volumes:
      - qdrant_data:/qdrant/storage
    networks:
      - portal-network
    healthcheck:
      test: ["CMD", "true"]
      interval: 30s
      timeout: 10s
      retries: 3
